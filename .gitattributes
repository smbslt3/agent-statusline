# Normalize line endings: LF in the repo for all text files.
* text=auto eol=lf

# Shell installers MUST stay LF or they break on macOS/Linux.
*.sh text eol=lf

# PowerShell installers are fine as LF too; keep them consistent.
*.ps1 text eol=lf
